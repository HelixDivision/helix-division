// Brand-specific icons (crosshair, shield/star, helix, wings, globe — the
// trust-bar iconography from the approved mockups). Reserved for marketing/
// trust-signal contexts only; functional UI chrome uses lucide icons via
// components/ui (see DESIGN_SYSTEM.md#core-components). Populated in Phase 2.
export {};
